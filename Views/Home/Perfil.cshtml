@model Enerfit.Models.Perfil

@{
    ViewBag.Title = "Mi Perfil";
}

<div class="container mt-4">
    <div class="card p-4 shadow-sm">
        <h2 class="text-center mb-4">👤 Mi Perfil</h2>

        @if (Model != null)
        {
            <ul class="list-group">
                <li class="list-group-item"><strong>Nombre:</strong> @Model.Nombre</li>
                <li class="list-group-item"><strong>Apellido:</strong> @Model.Apellido</li>
                <li class="list-group-item"><strong>Email:</strong> @Model.Email</li>
                <li class="list-group-item"><strong>Sexo:</strong> @Model.Sexo</li>
            </ul>

            <div class="text-center mt-4">
                <a href="/Home/EditarPerfil" class="btn btn-primary">Editar</a>
                <a href="/Home/CerrarSesion" class="btn btn-danger">Cerrar Sesión</a>
            </div>
        }
        else
        {
            <p class="text-danger text-center">No se encontraron datos del perfil.</p>
        }
    </div>
</div>
