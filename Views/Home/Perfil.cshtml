@model Enerfit.Models.Perfil

@{
    ViewBag.Title = "Mi Perfil";
}

<div class="perfil-container">
    <div class="perfil-card">
        <h5 class="perfil-titulo">👤 Mi Perfil</h5>

        @if (Model != null)
        {
            <div class="perfil-datos">
                <p><strong>Nombre:</strong> @Model.Nombre</p>
                <p><strong>Apellido:</strong> @Model.Apellido</p>
                <p><strong>Email:</strong> @Model.Email</p>
                <p><strong>Sexo:</strong> @Model.Sexo</p>
            </div>

            <div class="perfil-botones">
                <a href="/Home/EditarPerfil" class="btn btn-warning me-2">Editar</a><br>
                <br>
                <a href="/Home/CerrarSesion" class="btn btn-danger">Cerrar Sesión</a>
            </div>
        }
        else
        {
            <p class="text-danger">No se encontraron datos del perfil.</p>
        }
    </div>
</div>
