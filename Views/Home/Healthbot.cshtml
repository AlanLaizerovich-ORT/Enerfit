<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>  HealthBot - Enerfit</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>


    <div class="chatbot-wrapper">
        <div class="chatbot-container">
            <header>
                <h2> HealthBot <span>Enerfit</span></h2>
                
            </header>

            <div id="chat-box" class="chat-box">
                <div class="message bot-message">
                    <div class="avatar bot-avatar"><img src="" alt=""></div>
                    <div class="bubble"><img src="/Imagenes/Fitty.png" alt="">¡Hola! Soy tu asistente Fitty. ¿Cómo puedo ayudarte hoy?</div>
                </div>
            </div>

            <div class="input-area">
                <input type="text" id="userMessage" placeholder="Escribí tu mensaje...">
                <button id="sendBtn">Enviar</button>
            </div>
            <div class="buttons">
            
           <p>Para entender como usar el HealthBot, entra aca para leer el tutorial</p><a href="/Home/Tutorial"><button>Tutorial</button></a>
        </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $("#sendBtn").click(function () {
            sendMessage();
        });

        $("#userMessage").keypress(function (e) {
            if (e.which === 13) sendMessage();
        });

        function sendMessage() {
    var userMessage = $("#userMessage").val().trim();
    if (userMessage === "") return;

    // Mostrar mensaje del usuario
    $("#chat-box").append(`
        <div class="message user-message">
            <div class="bubble">${userMessage}</div>
            <div class="avatar user-avatar">🧍‍♂️</div>
        </div>
    `);
    $("#userMessage").val("");
    $("#chat-box").scrollTop($("#chat-box")[0].scrollHeight);

    //  Llamada AJAX al controlador
    $.ajax({
        type: "POST",
        url: "/Home/GetResponse",
        data: { userMessage: userMessage },
        success: function (data) {
            // Mostrar respuesta del bot
            $("#chat-box").append(`
                <div class="message bot-message">
                    <div class="avatar bot-avatar"><img src="/Imagenes/Fitty.png" alt=""></div>
                    <div class="bubble">${data.reply}</div>
                </div>
            `);

            // Si el bot pide redirigir, lo hace luego de 2 segundos
            if (data.redirect) {
                setTimeout(() => {
                    window.location.href = data.redirect;
                }, 2000);
            }

            $("#chat-box").scrollTop($("#chat-box")[0].scrollHeight);
        },
        error: function () {
            $("#chat-box").append(`
                <div class="message bot-message">
                    <div class="avatar bot-avatar"><img src="/Imagenes/Fitty.png" alt=""></div>
                    <div class="bubble">⚠️ Ocurrió un error al procesar tu mensaje.</div>
                </div>
            `);
        }
    });
}

    </script>
     
</body>
</html>